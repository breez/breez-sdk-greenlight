[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
panic = "abort"

[workspace]
members = [
  "sdk-core",
  "sdk-bindings",
]

[workspace.dependencies]
uniffi = "0.22.0"
uniffi_macros = "0.22.0"