name: Publish Android Bindings
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'breez-sdk repo release (MAJOR.MINOR.PATCH)'
        required: true
        type: string

jobs:
  publish-android:
    runs-on: macOS-latest
    steps:
      # - name: Install dependencies
      #   run: |
      #     sudo apt-get install -y protobuf-compiler
      #     cargo install --version 0.22.0 uniffi_bindgen
      #     cargo install cargo-ndk
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.version }}
      - name: Setup SSH key
        env:
          SSH_PRIVATE_KEY: ${{secrets.REPO_SSH_KEY}}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          sudo chmod 600 ~/.ssh/id_rsa
          ssh-add ~/.ssh/id_rsa
      # - name: Setup Java
      #   uses: actions/setup-java@v3
      #   with:
      #     distribution: temurin
      #     java-version: 11
      # - name: Setup Gradle
      #   uses: gradle/gradle-build-action@v2
      #   with:
      #     gradle-version: 7.3.2
      # - name: Build Android project
      #   env:
      #     SSH_AUTH_SOCK: /tmp/ssh_agent.sock
      #   run: |
      #     cd libs/sdk-bindings
      #     make init
      #     make bindings-android
      # - name: Publish artifacts
      #   uses: gradle/gradle-build-action@v2
      #   with:
      #     build-root-directory: libs/sdk-bindings/bindings-android
      #     arguments: publish -PlibraryVersion=${{ inputs.version }} -PbreezReposiliteUsername=${{ secrets.BREEZ_MVN_USERNAME }} -PbreezReposilitePassword=${{ secrets.BREEZ_MVN_PASSWORD }}
      - name: Publish artifacts
        run: |
          cd libs/sdk-bindings/bindings-android
          ./gradlew tasks          
          ./gradlew --info --debug publish -PlibraryVersion=${{ inputs.version }}
